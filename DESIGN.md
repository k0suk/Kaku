# 余白（Yohaku）V1 設計書

## 1. プロダクト概要

**余白**は、noteなどに投稿する長文記事を、静かで快適な環境で書くためのデスクトップ・テキストエディタである。文章をAIが代筆するのではなく、書き手が考えに集中し、迷わず書き始め、安心して保存・持ち出しできることを最優先にする。

V1はWindowsとmacOSで動作するローカルファーストのアプリとする。アカウント、クラウド同期、共同編集、AI機能、noteへの直接投稿は含めない。

## 2. ターゲットユーザー

- note、ブログ、SNS向けの長文を定期的に書く個人
- Wordやブラウザの編集画面では集中しにくい人
- Markdownに詳しくなくても、整った記事を書きたい人

## 3. 成功条件

ユーザーがアプリを開き、数秒以内に前回の原稿の続きを書けること。保存を意識せずに長文を書き、見出しで全体を把握し、Markdownまたはプレーンテキストとして持ち出せること。

## 4. プラットフォーム・技術方針

| 項目 | 方針 |
| --- | --- |
| 対応OS | Windows 10/11、macOS 13以降 |
| アプリ基盤 | Tauri 2 |
| UI | React + TypeScript |
| エディタ | TipTap（ProseMirror） |
| ローカル保存 | SQLite（Tauriプラグイン経由） |
| 記事形式 | 内部はJSONドキュメント、Markdown／TXTへ書き出し |
| 自動保存 | 編集後600msのデバウンス + アプリ終了前に保存 |
| 配布 | Windowsインストーラー、macOS DMG。macOSは署名・公証して配布 |

データは端末内にのみ保存する。ネットワーク接続を必要としない。

## 5. V1の機能範囲

### 含める

1. 記事の作成、編集、一覧表示、削除、復元
2. タイトル・本文のリッチテキスト編集
3. 段落、見出し（H2/H3）、箇条書き、番号付きリスト、引用、区切り線、太字、リンク
4. `# `、`## `、`- `、`1. `、`> ` など、主要なMarkdown入力ショートカット
5. 自動保存と保存状態の表示
6. 文字数・読了時間表示
7. 見出しから自動生成されるアウトラインと、クリックによる該当箇所への移動
8. フォーカスモード、ダークモード、文字サイズ・行間・本文幅の調整
9. 読み返しモード
10. Markdown／TXTの書き出し、クリップボードへのコピー
11. ゴミ箱（30日間復元可能）
12. 設定・データのバックアップ書き出し

### 含めない

- ログイン、同期、複数端末対応
- 共同編集、コメント
- AI生成・推敲
- 画像アップロード・画像編集
- noteその他サービスへの直接投稿
- テーマの細かなカスタマイズ、プラグイン機構

## 6. 情報設計と画面構成

### 6.1 メイン画面

デスクトップは3カラム。初期状態では左の一覧、中央のエディタ、右のアウトラインを表示する。ウィンドウ幅が狭い場合、右カラムはボタンで開くドロワーにし、左カラムは折りたたむ。

```
┌─────────────┬─────────────────────────────────┬──────────────┐
│ 左：記事一覧  │ 中央：執筆エリア                  │ 右：アウトライン │
│             │                                 │              │
│ ＋ 新しい記事 │ 書式ツールバー                    │ この記事      │
│             │ タイトル                          │ ・タイトル    │
│ 下書き       │ 本文（リッチテキスト）             │   ・見出し    │
│ ・記事A      │                                 │              │
│ ・記事B      │ 文字数・読了時間・保存状態          │ 投稿前チェック │
│             │                                 │              │
└─────────────┴─────────────────────────────────┴──────────────┘
```

#### 左カラム：記事一覧

- 上部に「＋ 新しい記事」ボタン
- セクションは「下書き」「ゴミ箱」
- 各行にタイトル、文字数、最終更新日時を表示
- 選択中の記事は淡い背景色で表示
- クリックで記事を切り替える。切り替え前に編集中の内容は必ず保存する
- ゴミ箱では復元と完全削除を提供する。完全削除は確認ダイアログを出す

#### 中央：執筆エリア

- 上部ツールバー：見出し、太字、箇条書き、番号付きリスト、引用、リンク、区切り線、元に戻す、やり直す
- ツールバー右端：保存状態（「保存中…」「保存済み」「保存に失敗」）
- タイトル入力欄：未入力時は「無題の記事」
- 本文：余白を広くとり、最大本文幅は読みやすさを優先する
- 下部：文字数と推定読了時間。日本語は400文字/分で算出し、最小1分とする

#### 右カラム：アウトライン

- 現在の記事のタイトルとH2/H3を階層表示
- 項目クリックで該当箇所へスクロールし、カーソルを置く
- 見出しがない場合は「見出しを追加すると、ここに記事の構成が表示されます」と表示
- 下部に投稿前チェックを置く。V1ではタイトル有無、本文文字数、見出し数のみを表示する

### 6.2 新規記事

- 「＋ 新しい記事」で空の記事を作成し、ただちに中央のタイトル欄へフォーカスする
- タイトルが未入力のままでも下書きとして保存する
- 初期本文には説明文を挿入しない。プレースホルダーは「ここから書く」のみ

### 6.3 読み返しモード

- 中央エディタ上部のメニューから切り替える
- 左右カラムと編集ツールバーを隠し、タイトルと本文だけを表示する
- Escまたは「編集に戻る」で編集モードに戻る
- V1では本文の編集は不可

### 6.4 設定・書き出し

- 右上メニューに集約する
- 表示設定：テーマ（システム／ライト／ダーク）、フォントサイズ（小／標準／大）、行間（標準／広い）、本文幅（標準／広い）
- 記事操作：Markdown書き出し、TXT書き出し、Markdownをコピー、ゴミ箱へ移動
- データ：全記事バックアップ（JSONまたはZIP）、バックアップから復元

## 7. 書き心地の設計

演出は控えめにし、設定で「モーションを減らす」を有効にしたOS環境では無効化する。

### 7.1 現在段落のフォーカス

- カーソルがある段落の背景を淡く強調し、他の段落は色を変えない
- フォーカスモード時だけ、カーソル位置から離れた段落を少し淡くする
- 色だけに依存せず、現在段落には2px程度の左アクセントも表示する

### 7.2 やわらかなスクロール追従

- 入力行がエディタ表示領域の下25%より下へ移動したとき、中央付近（およそ55%）へゆっくりスクロールする
- ユーザーがホイールやトラックパッドで手動スクロールしてから3秒間は自動追従を停止する
- 連続アニメーションは避け、1回の追従は200ms以内とする

### 7.3 保存のフィードバック

- 入力後は「保存中…」、保存成功で「保存済み」と表示する
- 保存済みへの変化は200ms程度のフェードのみ。通知、音、トーストは表示しない
- 保存失敗時は状態を赤字にし、再試行ボタンとエラー内容を表示する

### 7.4 アウトラインの即時更新

- 見出しが作成・編集・削除されたら、アウトラインも即時に更新する
- 新しい項目は短いフェードイン、削除は短いフェードアウトのみ

### 7.5 文字数の節目

- 500、1,000、2,000、3,000文字を初めて超えたときだけ、右下に小さなメッセージを3秒表示する
- 例：「1,000字を書きました」
- 目標設定、連続日数、ランキングなど、執筆を競争化する機能は入れない

## 8. 操作・ショートカット

| 操作 | Windows | macOS |
| --- | --- | --- |
| 保存 | Ctrl+S | Cmd+S |
| 元に戻す | Ctrl+Z | Cmd+Z |
| やり直す | Ctrl+Shift+Z | Cmd+Shift+Z |
| 太字 | Ctrl+B | Cmd+B |
| リンク | Ctrl+K | Cmd+K |
| 新しい記事 | Ctrl+N | Cmd+N |
| フォーカスモード | Ctrl+Shift+F | Cmd+Shift+F |
| 読み返しモード | Ctrl+Shift+R | Cmd+Shift+R |
| 編集に戻る | Esc | Esc |

## 9. データモデル

### Article

```ts
type Article = {
  id: string;                 // UUID
  title: string;
  contentJson: string;        // TipTap JSON
  createdAt: string;          // ISO 8601
  updatedAt: string;          // ISO 8601
  deletedAt: string | null;   // ゴミ箱に入れた日時
};
```

### Settings

```ts
type Settings = {
  theme: 'system' | 'light' | 'dark';
  fontSize: 'small' | 'medium' | 'large';
  lineHeight: 'normal' | 'relaxed';
  contentWidth: 'normal' | 'wide';
  focusMode: boolean;
};
```

### 必要なローカルDB操作

- 記事の作成・取得・更新・ゴミ箱移動・復元・完全削除
- 最終更新日時順の下書き一覧取得
- 30日を過ぎたゴミ箱の記事の自動完全削除（アプリ起動時に実行）
- 設定の取得・更新
- 全記事と設定のエクスポート・インポート

## 10. 保存・エラー処理

1. 編集内容をReactのローカル状態に反映する
2. 最後の入力から600ms後にSQLiteへ保存する
3. 保存開始時に「保存中…」、成功時に「保存済み」を表示する
4. 記事切替、新規作成、アプリ終了、ショートカット保存時は保留中の保存を直ちに実行する
5. 保存失敗時は編集内容をメモリに維持し、エラー表示と再試行を出す
6. アプリ終了時に未保存の場合は保存を試み、失敗した場合のみ終了確認を出す

## 11. アクセシビリティ・品質要件

- 全機能をキーボードで操作できること
- アイコンのみのボタンにはアクセシブルな名前を付与すること
- 色だけで状態を伝えないこと
- OSのダークモードと、モーション削減設定を尊重すること
- 100,000文字程度の記事でも編集・保存・アウトライン更新に体感上の遅延がないこと
- オフライン状態でもすべてのV1機能が動作すること
- 日本語入力（IME変換中）に干渉しないこと

## 12. 実装順序

1. Tauri + React + TypeScriptのプロジェクト作成、SQLite接続、基本レイアウト
2. 記事CRUDと自動保存、起動時の前回記事復元
3. TipTapエディタと基本書式、Markdown入出力
4. アウトライン、文字数・読了時間、ショートカット
5. フォーカス／読み返しモードと書き心地の演出
6. 設定、ゴミ箱、バックアップ
7. Windows/macOSでの手動QAとパッケージング

## 13. 受け入れ条件（V1完了判定）

- [ ] WindowsとmacOSで起動し、ネットワーク未接続でも使える
- [ ] 起動時に、前回開いていた記事が編集可能な状態で表示される
- [ ] 記事を作成・編集・削除・復元できる
- [ ] 編集中に保存操作をしなくても、アプリを再起動後に内容が残っている
- [ ] タイトルと本文から文字数・読了時間が正しく更新される
- [ ] H2/H3の追加・変更・削除がアウトラインへ反映され、クリックで移動できる
- [ ] Markdownの書き出しとクリップボードコピーができる
- [ ] フォーカスモード・読み返しモード・テーマ・文字サイズ・行間・本文幅が動作する
- [ ] 日本語IME入力、ショートカット、キーボード操作に問題がない
- [ ] 保存失敗時に、ユーザーが状況を理解して再試行できる

## 14. V2候補

- 任意ログインと暗号化クラウド同期
- iOS/iPadOS版
- note貼り付け向けの詳細プレビュー
- 画像の配置とキャプション
- 記事テンプレート
- 自分の過去記事を参照した文章チェック
